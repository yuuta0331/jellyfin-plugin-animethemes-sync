name: '📝 Release Notes Guidance'

on:
  workflow_dispatch:

jobs:
  guidance:
    runs-on: ubuntu-latest
    steps:
      - name: Print release notes guidance
        run: |
          echo "Release notes are read from the annotated git tag message."
          echo "Use short, user-facing bullet points before pushing a v* tag."
